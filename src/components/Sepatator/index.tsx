import React from 'react'
import { View } from 'react-native'

import { Separator } from './styles'

const MySeparator: React.FC = ({ ...rest }) => {
  return <Separator {...rest} />
}

export default MySeparator
